# ─────────────────────────────────────────────────────────────
#  network_credentials.env — 網路設備憑證 / network gear credentials
#  複製成 network_credentials.env 後填入（已被 .gitignore 忽略，永不入庫）
#  Copy to network_credentials.env and fill in (gitignored, never committed).
# ─────────────────────────────────────────────────────────────

# 交換器（SSH/Telnet）登入
# Switch (SSH/Telnet) login.
SWITCH_USERNAME=""
SWITCH_PASSWORD=""
SWITCH_ENABLE_PASSWORD=""
SWITCH_SNMP_COMMUNITY=""

# 網路電話總機 / PBX 管理介面
# IP-phone PBX management interface.
PBX_WEB_URL=""
PBX_USERNAME=""
PBX_PASSWORD=""

# 話機網頁管理（如適用）
# Per-phone web admin (if applicable).
PHONE_USERNAME=""
PHONE_PASSWORD=""
